# <项目名> · 需求文档编写项目档案（requirements-profile）

> 本文件供 `requirements-writer` skill 读取，把通用模板里的占位符替换成本项目真实术语。
> **新项目开一次、填完即可**。建议复制到项目仓库根、`.agents/` 或 `docs/` 下，命名为 `requirements-profile.md`。
> 填不清楚的先留空或写“无 / 单体”；skill 会按缺省情形裁剪结构，不会硬套不存在的层。

## 基本信息

- 项目名称：
- 仓库根说明文件：`README.md` / `docs/项目索引.md`（skill 会先读它了解全局）
- 产品视角 / 沟通约定（可选）：例“先说业务再说技术、面向产品同学”
- 默认文档强度：轻量 / 标准 / 审计（遇高风险需求时 skill 自动升级）

## 架构分层（占位符 → 本项目真实名）

| 占位符 | 本项目对应 | 说明 |
|---|---|---|
| `{{前端}}` |  | 例：`web`（React + Vite）/ `app`（移动端）；纯后端项目填“无” |
| `{{本地后端}}` |  | 例：`api-server` / 本地服务；单体后端也填这一格 |
| `{{云端后端}}` |  | 例：云端 / ERP 后端；无云端填“无” |
| `{{运行档位机制}}` |  | 例：一个开关切数据库 / 服务注册 / 白名单；无档位填“无” |

> 系统实际是几层就填几层。单体项目只填 `{{本地后端}}` 一格，其余写“无”。

## 数据库

| 占位符 | 本项目对应 |
|---|---|
| `{{主库}}` | 例：SQLite（本地）/ PostgreSQL / MySQL |
| `{{次库}}` | 例：SQL Server（云端）；单库填“无” |

- 用户隔离字段：例 `OwnerType` / `UID`
- 加列 / 建表约定：例 幂等建表 / `ALTER ADD COLUMN` / 迁移工具用法与注意点

## 鉴权与白名单术语

- `{{鉴权方案}}`：例 某种 scheme / JWT / Session / 单一鉴权
- `{{白名单机制}}`：例 按运行档位暴露控制器的白名单类；无则填“无”
- 计费机制：例 算力扣费 / 积分；无计费填“无计费”

## 验收 / 调试方式

- `{{验收方式}}`：例 某个本地调试 skill / `npm run dev` / `docker compose up` / 手动起服务

## 前端原型与视觉确认（有界面的项目必填）

从无到有且存在用户界面的需求，skill 会先写最小业务草案，再做一期「前端原型 + 模拟数据」交用户确认。**需求文档和全部期次一次写完**，只在期次序列上划一条视觉确认门槛线：原型期完成后唤起本地调试入口交用户实测，确认并复核后才施工后端。这里写清原型怎么跑、从哪个地址看、模拟数据放哪里，避免每次追问。

- 本地预览启动命令：例 `npm run dev` / `pnpm dev` / `docker compose up web`；纯后端项目填“无界面”
- 本地预览入口地址：例 `http://localhost:5173/xxx`；桌面端 / 移动端写实际打开方式（哪个调试包、怎么装、进哪个菜单）
- 预览前置条件：例 需先 `npm install`、需打开 mock 开关、需要测试账号；没有填“无”
- 模拟数据存放约定：例 `src/mocks/` 下按功能建目录，导出与真实接口同形状的数据；接后端时只替换数据来源，不改页面
- mock / 调试入口与生产隔离方式：例 `import.meta.env.DEV` + 依赖注入；生产构建测试必须确认无 mock 引用
- 前端组件与设计语言来源：例 组件库名 + 现有页面参考路径，原型必须沿用，不新造风格
- 原型允许触碰的范围：只做页面与交互；不建表、不写正式接口、不接鉴权、不做数据迁移
- 视觉确认结论记录位置：例 `docs/<功能名称>/开发提示词/期1-*.md` 的「前端确认记录」
- 视觉确认版本锚点：优先提交号；无 Git 时用不可覆盖的 `prototype-YYYYMMDD-HHmm-<短哈希>` + 文件清单 SHA-256 + 运行命令；同时记录页面入口和截图 / 录屏路径
- 技术探针约定：仅当数据、第三方、性能或安全事实会阻塞原型成立时设期 0；只产证据，不建设正式底座

## Git 代码检查点与远程备份

- 默认保存策略：不保存 / 本地提交 / 本地标签 / 远程分支
- 已批准的目标远程：例 `origin`；无固定授权填“无，逐次由用户明确授权”
- 分支策略：当前分支 / 功能分支 / Pull Request；不得仅因远程存在就自行选择
- 阶段标签前缀：`checkpoint/<feature-slug>/phase-N`
- 数据 / 外部状态回退约定：例 迁移脚本 + 备份恢复 / 补偿任务；无统一方案填“按功能单独设计”
- 敏感信息规则：真实密码、令牌、私钥、带口令连接串一律不得进入 Git，不接受上传授权

## 结构检索工具

- `{{结构检索工具}}`：例 CodeGraph（`codegraph_*`）/ 或“grep + 文件读取”

## 样本文档（本项目已有的好范例，供 skill 学结构）

- 主需求文档样本路径：例 `docs/xxx/xxx-开发需求文档.md`
- 分期提示词样本路径：例 `docs/xxx/开发提示词/README.md`、`期1-*.md`
- 若还没有样本：填“暂无，按 skill 内置模板结构写”

## 文档产出位置约定

- 默认放：`docs/<功能名称>/`（可改）

## 端口 / 目录约定（可选）

- 例：前端 dev 端口、后端端口、各前端产物目录……写清避免 AI 猜错。
