# 分期开发提示词模板

用这个模板写 `开发提示词/README.md` 和 `期N-*.md`。目标是让每期能单独开窗口执行，AI 读完就知道要看什么、改哪里、不能碰哪里、怎么验收。

> **术语替换（先读）**：占位符 `{{前端}}`、`{{验收方式}}`、`{{结构检索工具}}`、`{{主库}}`、`{{次库}}`、`{{鉴权方案}}`、`{{白名单机制}}` 从**项目档案**取真实值替换；项目没有的机制就删掉对应句子，不硬套。
>
> **范围冻结（必做）**：期次只能拆自主需求文档“本次范围”中已准入的能力。模板出现的输入源、搜索、批量、第三方、体验或平台扩展只是条件说明，不代表要创建对应期次。
>
> **业务共识前置（必做）**：先读主需求文档“0. 业务需求共识”。存在“阻塞确认”时不得生成或执行正式实现期；只有用户知情且目标就是解除该阻塞的最小期 0 探针或备选视觉原型可以先行，并必须标为“解除阻塞期”。每期业务目标都必须能用大白话回指第 0 节最终期望效果。
>
> 注意区分两种“未确认”：**业务共识阻塞**是方向本身没定，方案写出来也没有基准，因此连文档都不生成；**视觉形态待确认**是方向已定、页面形态待验，文档照常一次写完，只拦施工。
>
> **证据与决策前置（必做）**：分期提示词是对主需求文档的二次压缩。生成正式实现期前确认当前决策台账没有阻塞，标准 / 审计模式的证据销账无“无归属”；解除阻塞期只承接它要验证的冲突编号，不扩展范围。每期写清承接的 `U/P/A/E/D` 编号。执行优先级为“当前有效决策 → 已确认最终效果与验收场景 → 主需求文档 → 本期提示词 → 原话证据”。
>
> **视觉先行前置（必做）**：从无到有且有界面时，完整读取 `references/visual-first-workflow.md`。**期 0 到期 N 的全部期次一次写完，不因等待用户确认而停笔**；改为在期次序列上划出视觉确认门槛线，门槛线之前是原型与前端可见效果，之后是后端、数据、鉴权、集成和迁移。期 1 文档必须写“本地预览与用户确认”：真实启动命令、预览入口、用户测试路径和放行条件。用户确认绑定具体版本、修改项闭环、门槛线之后的期次复核完成后，才开始后端施工。
>
> **AI 工作量前置（必做）**：生成分期文档前完整读取 `references/phase-workload-planning.md`。先拆任务原子和依赖，再对每期做六维评分与平衡；普通开发期控制在 4–7 分，8 分以上必须继续拆。分期边界首先服务于单窗口工作量、上下文负载、风险隔离和中断恢复，不要求每期都是完整业务功能。
>
> **Git 代码检查点前置（必做）**：生成分期文档前完整读取 `references/git-backup-workflow.md`。从项目档案读取保存策略；远程存在不等于用户授权推送。真实敏感信息一律阻止进入 Git，代码标签不能代替数据和外部状态回退。

## 开发提示词 README

```markdown
# <功能名称> · 分期开发提示词

本目录是“<功能名称>”的分期开发提示词，配合上级目录的《<功能名称>-开发需求文档.md》使用。

## 用法
- 每期单独开一个开发窗口，把对应 `期N-*.md` 的全文作为初始提示词发给 AI。
- 每份提示词都限定核心必读材料、按需材料和一个主要问题，不要求新窗口重新加载全部项目历史。
- 新窗口先读上一期“阶段交接记录”和代码恢复锚点（如有），再读本期核心材料；不得依赖旧窗口的聊天记忆。
- 按数字顺序开发；每期完成并验收通过后，再进入下一期。
- 遇到“视觉确认门槛线”时停下施工：先按门槛线所在期的“本地预览与用户确认”把页面交用户实际测试，确认通过、修改闭环并复核下方期次后，再继续。文档本身已经全部写完，等待期间无需重写需求。
- 按 README 的保存策略建立代码检查点；只有已授权远程分支时才要求推送和远程验证。

## 期次总览
| 期 | 文档 | 要解决的独立问题 | 来源编号 | 工作量 | 上下文 | 依赖 | 独立产物 / 验收 | 代码恢复锚点 | 状态 |
|---|---|---|---|---:|---|---|---|---|---|

> 存在前端原型期时，在期 1 与下一期之间插入一行门槛线，让人一眼看出施工放行位置：
>
> `| — | — | **视觉确认门槛线：期1 本地预览确认通过、修改项闭环、下方期次复核完成后，才可开始下方施工** | — | — | — | — | — | — | — |`
>
> 门槛线之上是原型与前端可见效果，之下是后端、数据、鉴权、集成和迁移。**全部期次在同一次输出中写完；门槛线只拦施工，不拦文档。**

## 证据与决策基线
- 需求证据：轻量模式内嵌于主文档 / 标准或审计模式见 `docs/<功能名称>/00-需求证据与原话参考.md`
- 需求证据销账状态：无归属条目 0 条（YYYY-MM-DD 核对）
- 当前决策台账版本：<版本 / 日期>
- 当前有效决策：<D-001, D-002 ...>
- 高关注项处理：<U-014 → D-002，本次必须；U-027 → 待确认>
- 执行约定：当前有效决策优先；只有决策缺失、冲突或证据不足时才停下来确认，不让已被替代的旧原话覆盖新决定。

## 前端视觉确认基线
- 当前任务授权：只编写文档，不改产品代码 / 已获用户授权实际施工
- 是否需要前端原型期：需要（期1）/ 豁免
- 豁免依据（豁免时必填）：无用户可见界面 / 明确 BUG 且没有新增视觉结构或交互决策 / 完全复用已确认交互，理由：<具体事实>
- 是否需要期0技术探针：否 / 是，需核实 <阻塞视觉成立的技术事实>
- 视觉确认门槛线位置：期1 之后 / 期<N> 之后（多个前端可见期时写最后一个）
- 门槛线之前的期次：<期0, 期1 ...>
- 门槛线之后的期次：<期2 ... 期N>，状态为“方案已成型，待原型确认后复核”
- 原型承载页面与主链路：<本次要先给用户看的页面和关键流程>
- 本地预览启动命令：<项目档案 {{验收方式}} 的真实命令>
- 本地预览入口：<http://localhost:<端口>/<路径> 或桌面端 / 移动端实际打开方式>
- 模拟数据形态核对结论：<区分已有系统已核实 / 新功能待设计 / 未知；字段、数据量、响应时间、失败方式>
- 用户视觉确认状态：未开始 / 已确认（YYYY-MM-DD）/ 已确认但有修改项 / 用户要求重做
- 确认记录位置：`期1-<原型期名称>.md` 的“前端确认记录”
- 确认版本锚点：<提交号或版本标识 + 页面入口 + 截图 / 录屏路径>
- 门槛线之后期次的复核结论：未复核 / 无实质变更，原样执行 / 已更新 <期次> / 已作废重拆 <期次>
- 后端施工放行条件：用户在本地预览确认通过、修改项闭环、版本锚点完整、门槛线之后期次已复核；未满足时不得启动数据结构、正式接口与鉴权改动。

> **文档不等确认。** 全部期次在同一次输出中写完，门槛线之后的期次照常写清任务、数据模型、接口和验收，只是标注“待视觉确认复核”。不允许只交付前端期就停笔，把剩余需求留到确认之后再写。
>
> 用户在确认环节说的新话追加到《需求证据与原始描述参考》，同步更新当前决策台账、主需求文档 §0.4 和 §4，并据此复核门槛线之后的期次；不得只留在聊天里。

## 工作量平衡结论
- 评分维度：变更面 / 跨层依赖 / 不确定性 / 数据安全风险 / 验收复杂度 / 上下文负载，各 0–2 分。
- 普通开发期目标：4–7 分；本功能普通期范围：<最低分>–<最高分>。
- 8 分以上重期：无 / 有，已拆为 <期次>。
- 0–3 分轻期：无 / 有，已合并到 <期次> / 作为独立检查点保留，理由：<理由>。
- 平衡结论：<为什么不会出现一期过重、后续期次过轻>。

## Git 代码检查点基线

| 项目 | 当前值 |
|---|---|
| 文档生成时检查时间 | YYYY-MM-DD HH:mm（时区） |
| 保存策略 | 不保存 / 本地提交 / 本地标签 / 远程分支 |
| 本地 Git 状态 | 可用 / 不可用 |
| 远程写入授权 | 未授权 / 已授权（依据与时间） |
| 远程与分支 | <仅已授权时填写脱敏标识> |
| 远程可见性 | 私有 / 公开 / 未知 |
| 不可用原因 | 无 / <可验证原因> |
| 阶段标签规则 | `checkpoint/<feature-slug>/phase-N` / Git 不可用时“不适用” |
| 真实敏感信息 | 一律阻止进入 Git |
| 数据 / 外部状态回退 | 不涉及 / 见 <方案路径> |

> 远程存在或凭据可用不等于授权推送。只有当前任务明确授权或项目档案存在批准策略时才执行远程写入。

## 全局约束（每期都适用）
- 当前有效的 `D-xxx` 是执行真相；原话负责举证，不得让已被替代的旧说法覆盖当前决定。
- 用户反复强调的点属于高关注信号，必须显式处理；是否本期实现以当前决策和范围为准。
- 用户明确否定过的内容不得实现，哪怕“顺手就能做”“做了更完整”。
- 第 0 节出现新的修改或冲突时，先更新并重新确认主需求文档，再继续开发，不按旧提示词自行猜测。
- UI 沿用项目现有设计语言。
- 存在前端原型期时，未取得绑定具体版本的用户确认并完成期次复核前，不执行正式建表、接口、鉴权和迁移期的施工；这些期次的文档照常一次写完，标注“待视觉确认复核”。期 0 只核实阻塞视觉成立的事实，不建设正式底座。
- 不复制敏感密钥或连接串。
- 风格、命名、目录结构与现有代码保持一致。
- 只实现主需求文档已准入且分配给本期的能力；不得顺手增加功能、管理入口、配置项、数据记录或后续扩展。
- 发现范围外优化时不要实现，最多在交付说明中标为“可选建议（不纳入本期）”。
- 每期只解决一个主要问题。实际影响面、任务包或阅读材料超过评估时，停在可验证边界并重新拆期，不强行在当前窗口做完。
- 每期完成后填写本期文档的“阶段交接记录”；下一期从持久化代码、文档和代码恢复锚点（如有）恢复上下文。
- 涉及本地运行时，优先用项目约定的验收方式（{{验收方式}}）真机 / 真环境验证。
- 每期开始记录版本基线：Git 可用时记录起点提交和工作区；Git 不可用时记录目标文件清单与 SHA-256，并明确当前没有自动代码恢复锚点。用户原有改动不得覆盖、丢弃或混入本期保存范围。
- 每期验收通过后按保存策略完成敏感信息检查和代码检查点；只有已授权远程才推送。真实敏感信息不得进入 Git。
```

## 单期提示词结构

每份 `期N-*.md` 按下面结构写：

```markdown
# 期N 开发提示词 · <期次名称>

你是本项目的开发工程师。本期只解决：<一个独立问题>。<明确不做什么、依赖什么、完成后给下一期留下什么>。

## 本期业务目标（大白话）
<不用开发术语说明：本期完成后，谁可以在哪里做什么，会看到或得到什么结果；对应主需求文档 §0 的哪一项最终效果。>

## 本期要解决的独立问题
- 问题：<只写一个主要问题>
- 为什么现在解决：<它与前后期的依赖关系>
- 独立产物：<代码、契约、调研结论、迁移结果或测试证据>
- 完成后如何验证：<本期独立完成标准>

## 范围依据
- 明确需求：<对应主需求文档范围表中的条目>（来源编号：U-xxx / D-xxx）
- 必要配套：<如有，说明缺少它会阻断哪条主链路；没有则写“无”>（服务于：D-xxx）
- 当前有效决策：<D-xxx>
- 代码 / 外部事实：<E-xxx，如有>
- 本期承接的用户原话：
  > 【U-014】<原话原样引用，不改写>
  > 【U-027】<原话原样引用；属于高关注信号，按当前 D-xxx 结论处理>
- 本期不做：<列出容易被误加、但未准入或属于其他期次的内容>

> 实现时先执行当前有效决策。只有决策台账缺失、互相矛盾或无法解释验收场景时，才回查原话证据并停下来确认。

## 本期工作量评估

| 维度 | 分值 | 依据 |
|---|---:|---|
| 变更面 | 0–2 | <模块、文件与替换范围> |
| 跨层与依赖 | 0–2 | <涉及层级、外部依赖或异步链路> |
| 业务 / 技术不确定性 | 0–2 | <已知事实、需调研或需确认内容> |
| 数据 / 安全 / 可逆性风险 | 0–2 | <迁移、权限、密钥、删除与回退> |
| 验收复杂度 | 0–2 | <场景、环境与回归链路> |
| 上下文负载 | 0–2 | <需要同时理解的模块、规范与业务域> |
| **合计** | **<0–12>** | 普通开发期应为 4–7；否则说明拆分 / 保留理由 |

## 上下文边界

### 核心必读
1. 分期总览：`docs/<功能>/开发提示词/README.md`，读取工作量结论、Git 基线和前期代码恢复锚点。
2. 需求文档：`docs/<功能>/<功能>-开发需求文档.md` 的 §0 和 <本期直接相关章节>。
3. 需求证据参考：<轻量模式填写主文档 §0 或任务记录路径；标准 / 审计模式填写 `docs/<功能>/00-需求证据与原话参考.md`>。只读本期承接的 `U/P/A/E` 编号及相关高关注 / 否定项。
4. 上一期交接：<上一期文档的“阶段交接记录”；首期写“无”>。
5. 核心代码入口（控制在 2–6 个）：
   - <本期必须理解的文件 / 符号>

### 按需读取
- <出现什么问题时，再读取哪个文件 / 规范>

### 不要加载
- <与本期无关的历史文档、模块或后续期次；避免为了了解全局全部读入>

## 前置状态
<写清本期开始前系统应处于什么状态，以及本期不能破坏哪些已完成能力。>
<本期位于视觉确认门槛线之后时，这里必须写上“期<N> 前端原型的用户视觉确认已通过、修改项已闭环、本期已按确认结果复核”，并指向原型期文档的“本地预览与用户确认”和“前端确认记录”；README 标记为豁免时写明豁免依据。>
<本期复核状态：待复核（原型确认后填写）/ 无实质变更，原样执行 / 已按 <确认日期 + 版本锚点> 更新，变更点为 <内容>。>

## 预计阻塞点与确认时机
- <可能改变业务结果、权限、数据、接口或系统边界的问题；没有就写“当前无已知阻塞点”>
- <在哪一步之前必须确认，不能先做了再问>

## 开发前版本基线
1. Git 可用时，重新检查当前仓库、具名分支、目标远程和推送条件；与 README 不一致时先同步 README。记录起点提交 `BASE_SHA` 和期初 `git status --short`。
2. Git 不可用时，记录本期预计触达文件及其期初 SHA-256；明确“无 Git，当前没有自动代码恢复锚点”，不得擅自初始化仓库或打包整个目录。
3. 标出期初已有的用户改动；不得覆盖、还原或混入本期保存范围。与本期文件重叠且无法安全区分时才请求用户处理。

## 任务清单

普通开发期只安排 3–6 个核心任务包；单一极小改动可以更少。只保留本期实际涉及的层，不涉及的层不要为凑结构创建任务。

### <实际涉及的页面 / 服务 / 数据层>
1. <具体到文件、类、接口、字段、行为>

## 数据模型（涉及时）
<涉及则写实际变更；不涉及可省略，或一句话写“复用已有数据，无需改结构”。>

## 技术约束与边界
- <只写与本期实际任务相关的计费、密钥、旧代码、数据库等边界>
- <只写本期实际存在的失败、并发、异步或容量边界>
- <搜索、筛选、批量、外链、上传、分页等未准入时不得加入>

## 验收
用 {{验收方式}} 起本地环境：
1. <用户从页面入口开始的步骤>
2. <成功状态>
3. <失败 / 异常状态>
4. <下游链路或回归验证>

## 本地预览与用户确认（前端原型期 / 门槛线所在期必写）

本期代码完成后不要直接进入下一期。按以下步骤唤起本地调试，交用户实际测试。

### 唤起本地预览
- 启动命令：`<项目档案 {{验收方式}} 的真实命令，写完整，不写“启动开发服务器”这类空话>`
- 预览入口：`<http://localhost:<端口>/<路径>>` 或 `<桌面端 / 移动端实际打开方式>`
- 前置条件：`<依赖安装、mock 开关、测试账号等；没有就写“无”>`
- 注意事项：`<端口占用、需保持运行、如何退出；没有就写“无”>`

### 请用户测试的路径
| 序号 | 用户操作 | 应当看到 | 请用户重点看 |
|---|---|---|---|
| 1 | <从入口开始的第一步> | <页面状态或结果> | <布局、顺序、措辞是否符合预期> |
| 2 | <触发空数据的路径> | <空状态提示> | <提示是否说得清下一步做什么> |
| 3 | <触发加载的路径> | <加载中反馈> | <等待期间是否有明确反馈> |
| 4 | <触发失败的路径> | <失败提示与可重试方式> | <用户能否判断出了什么问题> |

### 放行条件（未满足不得开始下一期施工）
- 用户已实际打开预览并逐条走完上表：是 / 否
- 用户确认通过，或提出的修改已全部改完并复看：是 / 否
- 下方“前端确认记录”已填写且版本锚点完整：是 / 否
- 门槛线之后的期次已按确认结果复核：无实质变更 / 已更新 <期次> / 已作废重拆 <期次>

> 用户确认是长时间、跨会话的动作。等待期间不要回头重写需求文档：文档已经一次写完，这里只做确认与复核。

## 前端确认记录（仅前端原型期填写，交用户看过后回填）
- 演示时间与方式：
- 原型提交号或本地版本标识：<无 Git 时使用 `prototype-YYYYMMDD-HHmm-<短哈希>`，并记录文件清单 SHA-256 与运行命令>
- 页面入口：
- 用户是否实际打开本地预览并走完测试路径：
- 截图 / 录屏路径：
- 演示给用户看的页面与主链路：
- 用户确认通过的部分：
- 用户当场提出的修改（逐条列，改完需再确认）：
- 用户明确否掉的部分（写入主需求文档“明确不做”）：
- 留待后端完成后再看的部分：
- 用户新说的原话已追加入档的编号：
- 模拟数据中标注“数据形态未核实”的项，以及后续由哪一期负责核实：
- 修改后复核结果：
- 门槛线之后期次的复核结论：无实质变更，原样执行 / 已更新 <期次> / 已作废重拆 <期次>
- 确认结论：已确认且版本锚点完整，可放行后端施工 / 有修改项待闭环 / 用户要求重做原型

> 本节未填写、或门槛线之后期次未复核前，不得开始任何涉及数据结构、正式接口或鉴权的期次施工。

## 阻塞与动态拆期
- 新发现改变业务结果、权限、计费、数据归属、删除范围或系统边界时，停止实现并先确认。
- 实际工作量超过 7 分、核心任务包超过 6 个或上下文负载升为“高”时，停在可验证边界并拆分剩余任务。
- 只有缩小后的本期已经形成独立可验收结果，才能标记完成；不得为了保持原期数硬做完。
- 已完成期次和代码恢复锚点不改名、不覆盖；未开始期次可根据新依赖重新编号。

## 阻塞快照（仅阻塞时填写）
- 已完成：
- 尚未完成：
- 发现的事实与证据：
- 需要用户决定的问题：
- 不同选择的业务影响：
- 当前代码 / 数据是否处于安全状态：
- 恢复开发的第一步：

## 阶段交接记录（完成后填写；Git 保存启用时纳入本期提交）
- 本期解决的独立问题：
- 已确认的业务事实：
- 关键实现与取舍：
- 变更文件 / 模块：
- 接口、数据结构或配置变化：
- 验收命令与结果：
- 未完成项与明确不做：
- 遗留风险 / 已知问题：
- 本期是否为门槛线所在期；是则写：本地预览是否已交用户实测、用户确认结论、修改闭环状态、下方期次复核结论：
- 下一期开始前必须满足：
- 本期代码恢复锚点：提交 / `checkpoint/<feature-slug>/phase-N` / 无 Git，不适用

## Git 代码检查点与恢复（验收通过后按策略执行）
1. 读取 README 保存策略；“不保存”或 Git 不可用时，只记录验收结果、变更文件和期末 SHA-256，跳过第 2–6 步。不得宣称存在代码恢复锚点，也不得擅自初始化 Git 或制作含敏感信息的全目录压缩包。
2. Git 可用且保存策略要求保存时，检查本期待提交文件和待推送提交中的敏感信息；真实或高度疑似敏感信息一律阻止进入 Git，只报告路径与类型。
3. 使用动手前记录的 `BASE_SHA` 和期初工作区清单核对变化；只精确暂存本期文件，禁止 `git add .` / `git add -A`。
4. 保存策略包含提交时，创建 `checkpoint(<feature-slug>): phase N complete`。
5. 保存策略包含标签时，创建不可覆盖标签 `checkpoint/<feature-slug>/phase-N`；冲突时使用 `-r2` 等修订标签，不移动旧标签。
6. 只有保存策略为“远程分支”且已获得远程写入授权时，才推送分支和标签并验证；否则保留本地检查点。
7. 交付报告给出验收、分支、完整提交号、标签、本地 / 远程状态和未保存原因；无 Git 时给出变更文件及期初 / 期末哈希，但明确这些哈希只用于核对、不能恢复代码。
8. 推送失败时标记“开发验收通过，本地检查点完成，远程备份失败”，不得强推。

存在 Git 标签时，安全回看代码版本使用 `git switch -c restore/<feature-slug>-phase-N checkpoint/<feature-slug>/phase-N` 新建分支。无 Git 时没有自动恢复能力；如确需建立恢复机制，先取得用户授权并选择版本控制或项目批准的备份方式。数据库、生产数据和第三方状态按单独回退 / 补偿方案处理，Git 标签不能代替。
```

## 期次拆分方法

正式实现期先确认主需求文档第 0 节不存在“阻塞确认”，再按范围准入过滤，最后按 `references/phase-workload-planning.md` 拆任务原子、画依赖、评分和平衡。存在阻塞时只允许生成专门解除该阻塞、且用户已知情的最小期 0 探针或备选视觉原型；它不得夹带正式底座或其他需求。

- 分期首先服务于 AI 单窗口执行，不要求每期都是完整业务功能。
- 普通开发期建议控制在 4–7 分；8 分以上强制拆，0–3 分轻量期原则上合并或写明独立检查点理由。
- 从无到有且有界面时，期 0 到期 N 一次写完，并在期 1 之后划出视觉确认门槛线；门槛线之后的期次标注“待视觉确认复核”，用户确认并完成复核后才开始施工，不得只交付前端期就停笔。
- 同一业务能力可以拆成多个关联技术问题；不要为省事按前端 / 后端 / 数据库机械拆（前端原型期是为消除视觉不确定性设立的风险隔离期，不算机械切块），也不要把调研、大量实现、迁移和清理塞进同一期。
- 每期只有一个主要问题、3–6 个核心任务包，并留下可验证产物和阶段交接。
- Git 检查和本地代码检查点按保存策略执行；远程推送只在已授权时进行，不创建“Git 备份期”。
- 只有需求已确认删除旧体系时才单独创建清理期，并放在新链路稳定之后。
- 体验、更多输入源、识别、搜索、第三方、批量、横向平台等只有在主需求文档已准入时才能成为期次。
- 不创建“以后也许会做”的占位期；可选建议不进入期次总览。

## 必写边界

- 本期承接哪些 `U/P/A/E/D` 编号，以及高关注项的当前处理结论。
- 本期不做什么。
- 哪些旧功能不能碰。
- 哪些新功能必须回归。
- 涉及密钥 / 配置时，写清不能进入哪些层。
- 涉及新增接口时，写清是否加入运行档位白名单（`{{白名单机制}}`；无此机制则忽略）。
- 涉及计费时，写清哪些行为计费或不计费。
- 涉及用户数据时，写清如何按当前用户隔离。
- 涉及用户可见界面时，写清本期是原型期还是实现期；实现期要写清它依赖哪一期的视觉确认结论和复核状态，或 README 里的豁免依据。
- 本期是门槛线所在期时，写清本地预览启动命令、预览入口、请用户走的测试路径和放行条件。
- 写清六维工作量评分、上下文边界、预计阻塞点、动态拆期条件和阶段交接。
- 写清 Git 保存策略、远程写入授权、阶段标签、敏感信息硬门槛和数据 / 外部状态回退方案。

## 不同类型期次的额外要求

只有主需求文档已确认存在对应类型的工作时，才使用下面的小节；小节标题本身不是创建该期次的理由。

### 期0技术探针（仅技术事实阻塞视觉成立时）

- 完整读取 `references/visual-first-workflow.md` 的“技术可行性不确定时的期0”。
- 只核实会阻塞页面成立的数据、第三方、性能或安全事实，产出 `E-xxx` 证据。
- 保持最小、对业务与生产状态默认只读、可丢弃，不建设正式表、业务接口或生产链路；已授权的沙箱 / 测试环境可发受控请求，未经明确授权不写业务数据、不产生费用或外部副作用。
- 验收是证据足以支撑原型，下一期固定进入前端原型。

### 前端原型期（从无到有且有界面时固定为期1）

- 完整读取 `references/visual-first-workflow.md` 并逐项执行。
- 本期唯一问题是让用户基于可交互视觉锚点发现、补充和确认需求，不是交付完整功能。
- 原型写在正式前端目录，沿用现有设计语言，使用可替换且与生产隔离的 mock；全新项目使用预期投入生产的技术栈建立最小正式脚手架。
- 模拟数据区分已有事实、新设计和未知，覆盖字段、数据量、响应时间、失败方式以及正常 / 空 / 加载 / 失败状态。
- 本期是视觉确认门槛线所在期，必须写“本地预览与用户确认”：真实启动命令、预览入口、请用户走的测试路径和放行条件。缺任一项视为文档不合格。
- 用户确认绑定提交号或可复现的非 Git 版本锚点、页面入口和截图 / 录屏；确认后按“确认后的复核与冻结”复核门槛线之后的期次。
- 本期文档写完不等于工作暂停：门槛线之后的期次在同一次输出中一并写完，本期只负责施工和确认。

### 调研 / 业务确认期（不确定性为 2 时）

- 只在错误理解现状会直接导致返工时创建，不为简单任务增加空泛调研期。
- 产出现状地图、代码 / 数据证据、关键业务规则、待用户确认点和后续开发入口。
- 不在结论未确认时顺手实现某个分支。
- 验收是“证据和决策足以让下一期不依赖旧聊天继续”，不是提交大量代码。

### 数据地基期（涉及数据变更时）

- 写本期实际涉及的实体、数据上下文、建表脚本、索引、可空字段和用户隔离。
- 只有本期确实需要对外提供数据能力时才写 Controller、Service 和 API 封装。
- 涉及持久化时验收“重启后数据不丢”；涉及多用户数据时验收“换账号只看自己的数据”。

### 云端或第三方期（已确认涉及时）

- 写密钥只在云端 / 服务端。
- 本期存在跨层调用时，写调用方经现有鉴权通道（`{{鉴权方案}}`）访问。
- 按实际链路写同步 / 异步和失败状态，不为模板新增异步机制。
- 写接口调研结论；未实测就写开发前需确认。

### 批量 / 搜索期（已确认涉及时）

- 只有存在“先预览再选择”的已确认流程时，才写候选数据是否落库。
- 只有数据规模或接口约束确实需要时，才写分页 / 游标 / 去重 / 上限。
- 涉及批量处理时，写单条失败是否影响整批。
- 涉及候选外链时，复用项目已有打开方式。

### 清理旧体系期（已确认删除时）

- 先列“必须保留”。
- 再列“删除清单”，按后端、前端、表、脚本、文档分组。
- 要求用 {{结构检索工具}} / 搜索核对依赖，分批删、边删边编译。
- 验收必须包含新链路回归和旧引用全局搜索。

### 体验打磨期（已确认且需独立验收时）

- 只写范围表中已确认的体验项，按价值排序。
- 不引入新链路。
- 验收只覆盖本期实际改变的页面状态和交互，不顺带增加编辑、批量、筛选或分页。
