---
name: requirements-writer
description: 编写或评审面向 vibe coding 的软件需求、PRD、技术规格与分期开发提示词时使用。先从用户原话、代码和外部材料建立可追溯证据与当前决策；对从无到有且存在用户界面的能力，先用正式前端代码制作可交互视觉原型，让普通用户或产品经理基于看得见的效果澄清需求，再冻结正式技术方案；随后按 AI 单窗口负载拆成可验收、可恢复的期次，并加入安全的代码检查点。也适用于开发中途改需求、处理冲突、限制范围、避免上下文过载和生成 docs 下成套开发文档。
---

# 开发需求文档编写

把本 skill 当作“需求发现与 AI 施工协议”，不是传统 PRD 填表器。先让普通用户确认业务结果；从无到有且有界面时，先让用户看到并操作原型，再冻结完整需求和技术方案；最后把已确认范围拆成可独立验收、可中断恢复的 AI 执行单元。

## 核心原则

1. **当前决策是执行真相，原话只负责举证。** 原话档案保留历史和冲突，但不得覆盖用户后来明确确认的决定。
2. **视觉原型是需求发现工具。** 从无到有且存在用户界面时，第一个用户可评审的开发产物必须是可交互原型，不先建设正式后端底座。
3. **先冻结范围，再展开方案。** 只有明确需求和主链路必要配套进入本次交付。
4. **文档按风险分级。** 小而明确的需求不强制生成企业级全套材料；高风险、长会话和冲突需求保留完整证据链。
5. **每期只解决一个主要问题。** 结束时留下可验证产物、阶段交接和安全的代码恢复锚点。
6. **真实敏感信息绝不进入 Git。** 不因私有仓库或用户授权而上传密码、令牌、私钥或真实连接串。

## 第 0 步：读取项目档案

按顺序读取：

1. 仓库根、`.agents/` 或 `docs/` 下的 `requirements-profile.md`。
2. 找不到时读取 `CLAUDE.md`、`AGENTS.md`、`README.md` 和架构索引。
3. 仍不清楚且答案会改变系统边界或返工成本时，再询问用户。

项目档案格式见 `references/project-profile-template.md`。只使用项目实际存在的架构层、数据库、鉴权、计费和验收方式；不存在的章节直接裁剪。

## 选择文档强度

先选择最轻但足以控制风险的模式：

| 模式 | 适用情形 | 需求证据处理 |
|---|---|---|
| 轻量 | 单轮、目标明确、低风险、小范围修改 | 保存关键原话与明确否定项，允许把无关内容标为“非需求上下文” |
| 标准 | 新功能、多轮沟通、需要拆期或视觉确认 | 保存全部与需求有关的原话、强调、否定、冲突和决策来源 |
| 审计 | 鉴权、计费、隐私、删除、迁移、生产数据、合同约束，或需求历史复杂 | 完整证据链、逐条销账、版本与决策历史；原始敏感值仍必须脱敏 |

不要因为模板存在就把简单任务升级成重流程。用户明确要求逐字存档时采用审计模式，但仍要先脱敏。

## 建立证据与当前决策

需要追溯时读取 `references/raw-requirements-template.md`。

- `U-xxx`：用户需求原话；`P-xxx`：开发中途经确认收录的前序原话；`A-xxx`：AI 提议且用户已明确表态的结论。
- `E-xxx`：代码、数据、现有文档或外部资料证明的事实。
- `D-xxx`：当前生效的产品或技术决策，必须记录状态、依据、日期和被替代关系。
- 原话出现重复或强烈措辞时标为“高关注信号”，再由用户确认是否属于本次必须范围；不得仅凭重复次数自动提升优先级。
- 原话与当前文档冲突时，先查当前决策台账。只有台账缺失、互相矛盾或证据不足时才暂停询问用户。
- 允许的销账状态为：已写入、已明确不做、待确认、阻塞确认、非需求上下文。
- 任何凭据、个人敏感信息或不应进入仓库的内容用 `[已脱敏：类型]` 替换，并记录已脱敏；不要保留原值。

执行优先级固定为：**当前有效的 `D-xxx` → 已确认最终效果与验收场景 → 主需求文档 → 分期提示词 → 原话证据**。

## 视觉先行

涉及从无到有的用户界面或明显改变用户流程时，完整读取 `references/visual-first-workflow.md`。

默认顺序：

1. 形成原型前最小业务草案，只写用户、问题、入口、初步流程、明确不做、关键假设和待视觉确认问题。
2. 若技术可行性会阻塞页面成立，先做最小“期 0 技术探针”，只产出事实，不建设正式底座。
3. 期 1 用项目正式前端代码、现有设计语言和可替换模拟数据制作可交互原型；全新项目没有前端时，使用预期进入生产的技术栈建立最小正式脚手架，不做一次性演示页。
4. 用户基于原型补充、否定和确认需求；确认必须绑定提交号或版本、页面入口和截图 / 录屏。
5. 修改项闭环后，更新当前决策台账，冻结正式需求、验收场景、数据契约和技术方案。
6. 只有此后才规划正式后端、数据库、鉴权、集成、迁移和清理期次。

无用户界面、没有新增视觉结构或交互决策的明确 BUG（例如目标位置已知的文案纠错）、完全复用已确认交互的升级可以豁免。只要布局、操作顺序、页面状态或主流程发生明显变化，就不属于豁免。

读取代码和材料以理解现状属于常规需求调研，不自动成为期 0；只有需要独立验证且结果会决定原型能否成立时，才设技术探针。用户只要求编写或评审文档时，只规划原型期和确认门槛，不擅自修改产品代码；实际施工原型必须处于用户授权的“构建 / 修改”范围内。

## 范围准入

对候选内容标记：

1. **明确需求**：用户明确要求，或当前有效决策已经确认。
2. **必要配套**：缺少它会导致明确需求无法正确、安全完成或无法验收；写清服务于哪条需求。
3. **范围外**：可选优化、未来设想、模板示例或无证据支持的扩展。

只有前两类进入功能、技术设计、验收和期次。搜索、筛选、批量、分享、埋点、历史、调度、管理后台、更多输入源和平台扩展不得因模板示例自动加入。

## 主需求文档

读取 `references/requirements-doc-template.md`。主文档至少回答：

- 谁在什么场景遇到什么问题，最终得到什么结果。
- 当前范围、明确不做、当前有效决策和仍需确认的问题。
- 关键假设、验证方式和可观察的成功信号。
- 典型场景的前提、操作、结果和证明材料。
- 实际涉及的页面、数据、接口、鉴权、计费、迁移与质量门槛。

有视觉原型时采用两阶段状态：

- `v0.x 视觉发现草案`：只冻结原型所需的业务方向和边界，完整技术章节标为“待视觉确认后冻结”。
- `v1.0 已确认开发基线`：视觉确认和修改项闭环后，补齐并冻结验收、数据、接口和后续分期。

## 分期开发提示词

业务与视觉基线满足后，读取：

- `references/phase-workload-planning.md`
- `references/phase-prompt-template.md`
- `references/git-backup-workflow.md`

先拆任务原子和单向依赖，再规划期次。六维评分覆盖变更面、跨层依赖、不确定性、数据 / 安全风险、验收复杂度和上下文负载：

- 4–7 分是普通期建议区间，不是质量证明。
- 8 分以上必须继续拆；0–3 分在多期方案中原则上合并或说明独立检查点价值。
- 每期只解决一个主要问题，通常包含 3–6 个核心任务包。
- 每期结束时系统保持可构建、可验证、可继续开发；行为变化与相关测试放在同一期。
- 下一窗口只依赖代码、需求基线、阶段交接和代码恢复锚点，不依赖旧聊天记忆。

## Git 与安全

生成分期文档前按 `references/git-backup-workflow.md` 检查现场状态，但远程存在和凭据可用不等于用户授权推送。

- 项目档案必须声明：不保存 / 本地提交 / 本地标签 / 远程分支，以及允许的远程和分支策略。
- 未获得当前任务的明确远程写入授权时，只建立本地检查点。
- 代码提交和标签是“代码恢复锚点”，不宣称能回滚数据库、生产数据或第三方状态。
- 数据迁移和外部副作用需要单独的回退或补偿方案。
- 真实敏感信息一律阻止提交和推送；只备份脱敏后的安全改动。

## 工作流与参考路由

1. 读取项目档案与当前代码 / 文档事实。
2. 选择轻量、标准或审计模式。
3. 建立需求证据、当前决策台账、范围和冲突状态。
4. 有新视觉形态时先产出视觉发现草案和原型期；未确认前不生成已冻结的完整后端计划。
5. 视觉确认后更新证据与决策，完成正式主需求文档。
6. 按工作量、依赖、风险和上下文拆期，写验收、交接与代码检查点。
7. 输出前按 `references/review-checklist.md` 自检。

交付物按需要选择，不强制每次全部生成：

```text
docs/<功能名称>/
├── 00-需求证据与原话参考.md       # 标准 / 审计模式；轻量模式可精简
├── <功能名称>-开发需求文档.md     # v0.x 视觉发现草案 → v1.0 开发基线
└── 开发提示词/
    ├── README.md
    ├── 期0-技术探针.md            # 仅技术可行性阻塞视觉时
    ├── 期1-前端原型与视觉确认.md  # 从无到有且有界面时必须
    └── 期N-*.md                   # 视觉确认后生成正式实现期
```

项目没有样本文档时使用内置模板；有样本时只学习结构，不照抄旧路径、端口和过期技术事实。
