---
name: requirements-writer
description: 编写或评审软件功能的开发需求文档、PRD、技术需求说明、分期开发提示词时使用。适用任意项目：先用大白话总结用户的原始目的、原始目标、AI 当前理解的最终效果和多轮沟通冲突，再读项目档案 requirements-profile.md 获取架构分层、数据库、鉴权、验收方式、样本文档等术语；把已确认范围按 AI 单窗口可控工作量、上下文负载、风险和依赖拆成独立关联、可中断恢复的开发期次，并为每期加入工作量评分、阶段交接和可回滚 Git 备份。当用户要求“不要自创需求结构、按现有范例写、评估和平衡每期工作量、避免单期过重或上下文过长、把需求拆成开发期次、输出 docs 下的主需求文档与 开发提示词/期N 文件”时尤其适用。
---

# 开发需求文档编写

用一套固定、可复用的结构把一个新功能的需求写清楚：先用普通用户能看懂的语言确认原始诉求、最终效果和需求冲突，再固定产品决策和范围依据，最后写实际涉及的技术设计与开发提示词。把分期视为 AI 执行单元：每期工作量和上下文可控、能独立验收、能在阻塞后恢复，并通过阶段交接和 Git 标签把结果可靠地传给下一窗口。

本 skill **不绑定任何具体项目**。项目相关的架构名、数据库、鉴权、验收方式、样本文档，全部从**项目档案**读取；模板里的占位符按项目档案的真实值替换。

## 第 0 步：读取项目档案（必做）

动手前先确定本项目的术语和约定，按顺序找：

1. 项目仓库里的 `requirements-profile.md`（常见位置：仓库根、`.agents/`、`docs/`）。这是本 skill 的配套档案，格式见 `references/project-profile-template.md`。
2. 找不到档案，就读项目的 `CLAUDE.md` / `README.md` / 架构索引文档，提取等价信息。
3. 还是不清楚，就先问用户这几件事再动手：
   - 系统分几层（单体？前后端？前端 + 本地后端 + 云端？），各层叫什么。
   - 用什么数据库，是否多库，用户数据怎么隔离。
   - 有没有鉴权 / 白名单 / 计费机制，叫什么。
   - 用什么方式做本地验收（跑命令 / 调试脚本 / 容器 / 专用 skill）。
   - 有没有已经写好的需求文档样本可以学结构。

把读到的值记下来。后面模板里的占位符——`{{前端}}`、`{{本地后端}}`、`{{云端后端}}`、`{{主库}}`、`{{次库}}`、`{{鉴权方案}}`、`{{白名单机制}}`、`{{验收方式}}`、`{{结构检索工具}}`——都用项目档案的真实值替换。

**关键原则：项目档案里没有的层 / 库 / 机制，就删掉模板里对应的小节，不要为凑结构硬造需求。** 单体项目就写单层，无云端就不写云端，无计费就明确“不计费”。

## 业务共识确认（必做）

主需求文档开头必须先写“业务需求共识”，并在任何架构、接口、数据或分期内容之前完成。按对话发生顺序提取：

1. **原始目的**：用户为什么提出需求，想解决什么业务问题。不要把“新增接口 / 改表”当成目的。
2. **原始目标**：用户最开始希望谁在什么场景下完成什么事、得到什么结果。保留原始版本，不用最终方案覆盖它。
3. **最终期望效果**：根据用户后续明确补充、修改或确认，写出 AI 当前理解的最终业务结果，包括使用者、操作入口、关键动作、看到的结果和明确不变的内容。
4. **需求演变与冲突**：逐项记录后续内容是补充、明确替换，还是与原始目标冲突，并标记“已确认 / 待确认 / 阻塞确认”。

全部使用普通业务语言，让不了解代码、接口和数据库的人也能判断“这是不是我想要的”。本节不写类名、方法名、表名、字段名、API 路径、队列等开发术语；确实无法避免时，先说业务影响，再在括号里补技术名。

如果只有一轮需求，就以用户当前原话提取原始目的和目标，并写“暂无需求演变或冲突”；用户没有说明的目的不要代替用户编造，标为“未明确说明，待确认”。

最新的明确修改或确认可以更新“最终期望效果”，但不能抹掉原始目标。代码现状和技术限制只能作为实现约束，不能擅自改写业务目标。发现会改变使用者、业务结果、主流程、权限、计费、数据归属、删除范围或系统边界的未解决冲突时，先向用户确认；在确认前标为“阻塞确认”，不得把任一冲突分支写成已确认范围或拆入开发期次。若用户只要求先出草案，保留冲突记录，并把分期标为“待业务共识确认后再拆分”。

## 范围准入（必做）

写架构和拆期前，先给每个候选功能、技术改动和验收项标记依据：

1. **明确需求**：用户明确要求，或现有需求 / 代码已确认。
2. **必要配套**：本身不是新增产品能力，但缺少它会导致主链路无法正确、安全地完成或无法验收。写清它依赖哪条明确需求。
3. **范围外**：只是可选优化、未来设想、模板示例，或没有证据支持。

只有“明确需求”和“必要配套”能进入本次功能、任务清单、数据 / 接口设计、验收标准和开发期次。“范围外”默认省略；确有提醒价值时，只能放进“可选建议（不纳入本次）”，不能混入开发计划。

不要因为模板提到某类能力，就自动增加入口、角色、配置、搜索、筛选、批量、更多输入源、分享、埋点、历史、调度、管理后台或横向平台。错误处理、兼容性、可访问性、测试和回归可以作为必要质量保障，但只覆盖本次变更直接触达的链路，不能借此扩展产品能力或重构无关模块。

需求存在歧义时，优先选择能完成目标的最小闭环。只有当不同选择会明显改变产品行为、系统边界、数据结构或返工成本时才提问；不要自行采用更大的方案。

## 工作流

1. 先读项目背景：项目档案、仓库根说明文件、相关现有需求文档、目标模块代码或用户给出的原始材料。
2. 判断交付物：
   - 只写完整需求文档：读取 `references/requirements-doc-template.md`。
   - 只写分期开发提示词：读取 `references/phase-workload-planning.md`、`references/phase-prompt-template.md` 和 `references/git-backup-workflow.md`。
   - 需要成套文档：读取需求模板、工作量规划、分期模板和 Git 备份工作流，并输出主文档 + `开发提示词/README.md` + `期N-*.md`。
   - 评审已有分期文档：读取 `references/review-checklist.md`、`references/phase-workload-planning.md` 和 `references/git-backup-workflow.md` 逐项核对。
3. 先重建业务共识：
   - 按对话顺序提取原始目的、原始目标、AI 当前理解的最终效果、需求演变与冲突。
   - 用大白话写在主需求文档最前面；没有历史对话时也不能省略。
   - 遇到影响业务结果或系统边界的未解决冲突，先确认；未确认时停止拆期。
4. 从材料中提取“已确认”和“待确认”，并执行范围准入：
   - 已由用户明确说过、现有代码已证明、已有文档明确写出的，写入“关键决策 / 已确认决策”。
   - 没有证据的实现选择、计费策略、删除范围、生产数据处理、第三方接口能力，写入“待确认 / 风险”，不要伪装成已确认需求。
   - 为每个拟写入文档的功能点标记“明确需求 / 必要配套”；没有准入依据的内容移出本次范围。
5. 先写“本次范围 / 明确不做”，冻结产品边界，再展开技术设计。
6. 按项目档案的架构分层写边界：只说明本次实际触达的层及其数据、密钥、计费、鉴权、异步任务归属。层数以档案为准，不硬套三层。
7. 把已准入的功能点落到可执行项：逐项判断是否涉及页面、接口、服务、表结构、配置、白名单、构建 / 打包脚本、旧代码清理和验收路径；不涉及的类型写“不变 / 不涉及”即可，不为填模板新增设计。
8. 生成分期提示词前，按 `references/phase-workload-planning.md` 把已准入工作拆成任务原子和单向依赖，再规划 AI 执行期次：
   - 分期边界优先服务于单窗口工作量、上下文负载、风险隔离和中断恢复，不按业务菜单或前端 / 后端 / 数据库机械切块。
   - 对每个候选期次做六维评分；普通开发期控制在 4–7 分，8 分以上必须继续拆，0–3 分轻量期原则上合并或写明独立检查点理由。
   - 每期只解决一个主要问题，限制为 3–6 个核心任务包，并写核心必读、按需读取、预计阻塞点和阶段交接记录。
9. 按 `references/git-backup-workflow.md` 检查当前仓库、分支、远程访问和推送能力，把脱敏结果写入分期 README：
   - Git 云端备份可用时，每期都加入“验收通过 → 敏感信息检查 / 授权 → 精确暂存 → 提交 → 阶段标签 → 推送并验证”的完成门槛。
   - 不可用时记录可验证原因，不自动初始化仓库、创建远程、改远程或索要凭据；每期开始时允许重新检查。
   - Git 备份是开发保障，不是产品能力，不单独拆期，也不作为扩大需求范围的依据。
10. 输出前自检：业务共识是否能让普通用户确认，冲突是否已显式处理，每项功能是否有准入依据，以及是否有明确不做、必要的复用 / 弃用清单、实际涉及的数据与接口、分期工作量评分与平衡结论、上下文边界、阻塞恢复、阶段交接、验收步骤、Git 备份基线和风险对策。

## 写作规则

- 主文档开头先讲用户为什么要做、最初想要什么、最终会得到什么，再给技术细节。
- “业务需求共识”必须使用普通小白能听懂的业务语言：用户要能知道“谁来用、点哪里、做什么、最后看到什么结果”，不能用开发术语代替业务结果。
- 技术方案必须服务于已确认的最终期望效果；如果技术章节与业务共识不一致，以业务共识为准并修正技术章节，不能静默改变业务目标。
- 多平台、多档位、多场景的结论分情况列举，避免“一律 / 全部”一刀切；先分情况，再给主推荐。
- 区分“产品能力”（用户得到什么）与“实现手段”（怎么做到）；改实现手段时说明产品能力不变，别把取舍说成砍功能。
- 先冻结范围再展开方案；不得把待确认问题、可选建议或模板示例写成已排期功能。
- 分期首先按 AI 的执行容量、上下文负载、风险和依赖划分，不要求每期都形成完整业务功能，也不允许用“核心期”掩盖单期工作量过重。
- 工作量评分只用于规划 AI 执行负担，不估算工时；发现一期实际影响面超出评估时，停在可验证边界并动态拆分，不强行在原窗口做完。
- 每期用持久化代码、文档、阶段交接和 Git 标签传递上下文，不把“记住上一窗口聊过什么”当作前置条件。
- 不新造项目结构。优先沿用项目现有的分层、鉴权、数据库边界、既有 UI 组件风格（以项目档案和现有代码为准）。
- 不把密钥、连接串、密码写进文档。只能写“配置在云端 / 本地配置 / 见私有配置”，不要复制敏感值。
- Git 远程地址、敏感信息检测结果和备份报告只写脱敏标识、文件路径与敏感类型，不回显令牌、密码、私钥或带凭据的 URL。
- 发现待备份改动含真实敏感信息时，按 `references/git-backup-workflow.md` 只询问一次；把允许 / 拒绝决定限定并持久化到“当前仓库 + 当前远程 + 本功能全部期次”，后续期次不得重复打扰用户。
- 每期备份只纳入本期文件，不覆盖、不丢弃、不偷偷提交用户期初已有改动；禁止用强推或移动旧阶段标签伪造备份成功。
- 涉及删除和迁移时保持高风险表达：先列保留清单和删除清单，再要求用项目的结构检索工具（`{{结构检索工具}}`）/ 搜索核对依赖、分批删除、边删边编译。
- 涉及第三方接口时保持可验证表达：记录调研日期、接口路径、方法、关键参数、返回字段、权限 / 计费、易踩坑；没有实测就标“需调研”。
- 研究参考项目或竞品实现时，先用目录树 / grep 摸清架构，深读到关键实现代码再下结论，给文件 + 行号佐证；不靠 README、文件名或片段判断技术路线。
- 涉及数据库变更时保持运行档位意识：不同档位 / 不同库的建表、迁移、唯一索引、用户隔离可能不同，必须写清楚（按项目档案的数据库情况）。
- 对前端保持实际体验导向：只覆盖本次主链路实际触达的入口、状态、反馈和回归路径；编辑、批量、筛选、分页、外链等只在已准入时写入，不把检查清单当成功能清单。
- 参考旧样本时只学结构，不机械照抄旧路径和旧端口；项目当前端口、目录和运行档位以项目档案与仓库根说明文件为准。

## 推荐路径

新功能文档默认放在：

```text
docs/<功能名称>/
├── <功能名称>-开发需求文档.md
└── 开发提示词/
    ├── README.md
    ├── 期1-<地基或最小闭环>.md
    ├── 期2-<核心能力>.md
    └── 期N-<后续已确认能力>.md
```

如果用户只要一份文档，不强行拆目录；如果用户要求“方便后续开发”，可以给成套结构。期次数量由已确认范围、任务原子、工作量评分、上下文负载、风险和依赖共同决定：小改动可用一期完成，但单期评分超过上限时必须继续拆；不为凑目录增加体验期、扩展期或未来功能期。

## 参考样本

需要看完整样本时，读**项目档案里登记的样本文档**（不同项目路径不同）。如果项目还没有可参考的样本文档，就完全按 `references/` 下的模板结构写，不要去找不存在的样本文件。
