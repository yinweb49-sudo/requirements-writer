---
name: requirements-writer
description: 编写或评审软件功能的开发需求文档、PRD、技术需求说明、分期开发提示词时使用。适用任意项目：先读项目档案 requirements-profile.md 获取本项目的架构分层、数据库、鉴权、验收方式、样本文档等术语，再用固定结构把业务需求拆成可执行、可独立验收的开发期次，输出主需求文档与分期开发提示词。当用户要求“不要自创需求结构、按现有范例写、把需求拆成开发期次、输出 docs 下的主需求文档与 开发提示词/期N 文件”时尤其适用。
---

# 开发需求文档编写

用一套固定、可复用的结构把一个新功能的需求写清楚：先固定业务目标、产品决策和边界，再写架构、交互、数据、接口、复用/清理清单，最后拆成可独立执行和验收的开发提示词。

本 skill **不绑定任何具体项目**。项目相关的架构名、数据库、鉴权、验收方式、样本文档，全部从**项目档案**读取；模板里的占位符按项目档案的真实值替换。

## 第 0 步：读取项目档案（必做）

动手前先确定本项目的术语和约定，按顺序找：

1. 项目仓库里的 `requirements-profile.md`（常见位置：仓库根、`.agents/`、`docs/`）。这是本 skill 的配套档案，格式见 `references/project-profile-template.md`。
2. 找不到档案，就读项目的 `CLAUDE.md` / `README.md` / 架构索引文档，提取等价信息。
3. 还是不清楚，就先问用户这几件事再动手：
   - 系统分几层（单体？前后端？前端 + 本地后端 + 云端？），各层叫什么。
   - 用什么数据库，是否多库，用户数据怎么隔离。
   - 有没有鉴权 / 白名单 / 计费机制，叫什么。
   - 用什么方式做本地验收（跑命令 / 调试脚本 / 容器 / 专用 skill）。
   - 有没有已经写好的需求文档样本可以学结构。

把读到的值记下来。后面模板里的占位符——`{{前端}}`、`{{本地后端}}`、`{{云端后端}}`、`{{主库}}`、`{{次库}}`、`{{鉴权方案}}`、`{{白名单机制}}`、`{{验收方式}}`、`{{结构检索工具}}`——都用项目档案的真实值替换。

**关键原则：项目档案里没有的层 / 库 / 机制，就删掉模板里对应的小节，不要为凑结构硬造需求。** 单体项目就写单层，无云端就不写云端，无计费就明确“不计费”。

## 工作流

1. 先读项目背景：项目档案、仓库根说明文件、相关现有需求文档、目标模块代码或用户给出的原始材料。
2. 判断交付物：
   - 只写完整需求文档：读取 `references/requirements-doc-template.md`。
   - 只写分期开发提示词：读取 `references/phase-prompt-template.md`。
   - 需要成套文档：两个 reference 都读，并输出主文档 + `开发提示词/README.md` + `期N-*.md`。
   - 评审已有文档：读取 `references/review-checklist.md` 逐项核对。
3. 从材料中提取“已确认”和“待确认”：
   - 已由用户明确说过、现有代码已证明、已有文档明确写出的，写入“关键决策 / 已确认决策”。
   - 没有证据的实现选择、计费策略、删除范围、生产数据处理、第三方接口能力，写入“待确认 / 风险”，不要伪装成已确认需求。
4. 按项目档案的架构分层写边界：每一层负责哪些数据、密钥、计费、鉴权、异步任务，分别归谁。层数以档案为准，不硬套三层。
5. 每个功能点都落到可执行项：涉及哪些页面、接口、服务、表结构、配置、白名单、构建 / 打包脚本、旧代码清理、验收路径。
6. 输出前自检：是否有业务目标、明确不做什么、复用 / 弃用清单、数据与接口、分期依赖、验收步骤、风险对策。

## 写作规则

- 先讲业务影响和结论，再给技术细节。
- 保持产品语言可读：用户要能知道“做完后能干什么、点哪里、看到什么结果”。
- 多平台、多档位、多场景的结论分情况列举，避免“一律 / 全部”一刀切；先分情况，再给主推荐。
- 区分“产品能力”（用户得到什么）与“实现手段”（怎么做到）；改实现手段时说明产品能力不变，别把取舍说成砍功能。
- 不新造项目结构。优先沿用项目现有的分层、鉴权、数据库边界、既有 UI 组件风格（以项目档案和现有代码为准）。
- 不把密钥、连接串、密码写进文档。只能写“配置在云端 / 本地配置 / 见私有配置”，不要复制敏感值。
- 对删除和迁移保持高风险表达：先列保留清单和删除清单，再要求用项目的结构检索工具（`{{结构检索工具}}`）/ 搜索核对依赖、分批删除、边删边编译。
- 对第三方接口保持可验证表达：记录调研日期、接口路径、方法、关键参数、返回字段、权限 / 计费、易踩坑；没有实测就标“需调研”。
- 研究参考项目或竞品实现时，先用目录树 / grep 摸清架构，深读到关键实现代码再下结论，给文件 + 行号佐证；不靠 README、文件名或片段判断技术路线。
- 对数据库保持运行档位意识：不同档位 / 不同库的建表、迁移、唯一索引、用户隔离可能不同，必须写清楚（按项目档案的数据库情况）。
- 对前端保持实际体验导向：入口、空状态、加载、失败、编辑、批量、筛选、外链打开、回归路径都要落到验收。
- 参考旧样本时只学结构，不机械照抄旧路径和旧端口；项目当前端口、目录和运行档位以项目档案与仓库根说明文件为准。

## 推荐路径

新功能文档默认放在：

```text
docs/<功能名称>/
├── <功能名称>-开发需求文档.md
└── 开发提示词/
    ├── README.md
    ├── 期1-<地基或最小闭环>.md
    ├── 期2-<核心能力>.md
    └── 期N-<体验或扩展>.md
```

如果用户只要一份文档，不强行拆目录；如果用户要求“方便后续开发”，优先给成套结构。

## 参考样本

需要看完整样本时，读**项目档案里登记的样本文档**（不同项目路径不同）。如果项目还没有可参考的样本文档，就完全按 `references/` 下的模板结构写，不要去找不存在的样本文件。
