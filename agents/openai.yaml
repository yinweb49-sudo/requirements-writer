interface:
  display_name: "开发需求文档编写"
  short_description: "平衡 AI 分期工作量与上下文，并加入可回滚 Git 备份"
  default_prompt: "Use $requirements-writer to confirm the business goal, balance each AI development phase by workload and context, preserve durable handoffs, and add verified Git backup checkpoints."
